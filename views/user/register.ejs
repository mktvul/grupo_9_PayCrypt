<!DOCTYPE html>
<html lang="en">
  <%- include ('../partials/head.ejs', {titulo: 'Registro', cssSheets:
  'styles_register'}) %>

  <body>
    <%- include ('../partials/header.ejs') %>
    <main>
      <!-- Form container-->
      <div class="form-container">
        <!-- PayCrypt logo-->
        <div class="logo-register">
          <img src="/images/paycrypt-logo.svg" alt="" />
        </div>

        <div>
          <h1>¡Hola! Ingresá tus datos</h1>
        </div>

        <!-- CAJA FLEX DEL  FORMULARIO-->
        <form
          class="form"
          action="/user/register"
          method="POST"
          enctype="multipart/form-data"
        >
        <div>
          <h2>Información personal</h2>
        </div>
          <div>
            <label for="name"></label>
            <input type="text" name="name" id="name" placeholder="Nombre" />
            <!--NOMBRE USUARIO-->
            <% if (locals.errors && errors.name) { %>
            <p class="text-danger"><%= errors.name.msg %></p>
            <% } %>
          </div>

          <div>
            <label for="lastName"></label>
            <input
              type="text"
              name="lastName"
              id="lastName"
              placeholder="Apellido"
            />
            <!-- APELLIDO-->
            <% if (locals.errors && errors.lastName) { %>
            <p class="text-danger"><%= errors.lastName.msg %></p>
            <% } %>
          </div>

          <div>
            <label for="dni"></label>
            <input type="number" name="dni" id="dni" placeholder="DNI" />
            <!-- NUMERO DE DOCUMENTO (DNI)-->
            <% if (locals.errors && errors.dni) { %>
            <p class="text-danger"><%= errors.dni.msg %></p>
            <% } %>
          </div>
          <div>
            <label for="location"></label>
            <input
              type="text"
              name="location"
              id="location"
              placeholder="Ubicación"
            />
            <p class="text-danger"></p>
          </div>
          <div>
            <h2>Información de cuenta</h2>
          </div>
          <div>
            <label for="email"></label>
            <input type="email" name="email" id="email" placeholder="E-mail" />
            <!-- CORREO ELECTRONICO-->
            <% if (locals.errors && errors.email) { %>
            <p class="text-danger"><%= errors.email.msg %></p>
            <% } %>
          </div>

          <div>
            <label for="password"></label>
            <input
              type="password"
              name="password"
              id="password"
              placeholder="Contraseña"
            />
            <!-- CONTRASEÑA-->
            <% if (locals.errors && errors.password) { %>
            <p class="text-danger"><%= errors.password.msg %></p>
            <% } %>
          </div>
          <div>
            <label for="password1"></label>
            <input
              type="password"
              name="password1"
              id="password1"
              placeholder="Confirmar Contraseña"
            />
            <!-- CONTRASEÑA-->
            <% if (locals.errors && errors.password1) { %>
            <p class="text-danger"><%= errors.password1.msg %></p>
            <% } %>
          </div>

          <div class="form-button-image">
            <label for="image">
              <div class="img-up">
                <i class="fas fa-upload"></i> Subir Imagen
              </div>
            </label>
            <!-- IMAGEN DEL PRODUCTO-->
            <input type="file" name="image" accept=".jpg" />
            <% if (locals.errors && errors.image) { %>
            <p class="text-danger1"><%= errors.image.msg %></p>
            <% } %>
          </div>

          <div class="form-button">
            <div class="form-button-checkbox">
              <input
                id="form-button-checkbox-input"
                type="checkbox"
                name="terminos"
                required=""
              />
              <label for="terminos"
                >He leído y acepto los Términos y Condiciones.</label
              >
              <!-- CAJA TERMINOS Y CONDICIONES-->
            </div>

            <div class="form-button-done">
              <button type="submit">Continuar</button>
            </div>

            <div class="form-button-login">
              <a role="button" href="/user/login"
                ><button type="button">Iniciar sesión</button></a
              >
            </div>
          </div>
        </form>
      </div>
    </main>
  </body>
</html>
